node_modules
dist
dist-ssr
.env
.env.local
.env.*.local
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.log
.firebase/
*.tsbuildinfo
worker/.wrangler
worker/node_modules
worker/dist
.serena/
android/
ios/
dist-apk/
# Keep android-keystore/ tracked — debug keystore is shared by design so the
# SHA-1 registered in Firebase stays stable across CI builds.
!android-keystore/
